syntax = "proto3";

package stream;

service Stream {
  rpc Test (stream Request) returns (stream Response) {}
}

message Request {
  uint32 id = 1;
  bytes payload = 2;
}

message Response {
  uint32 id = 1;
  bytes payload = 2;
}
